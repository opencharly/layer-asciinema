version: 2026.232.0520
asciinema:
    candy:
        version: 2026.144.1443
        description: |
            Record and replay terminal sessions as asciicast v2 .cast files, and
            render them to animated GIFs with agg
            The asciinema candy installs the asciinema terminal recorder from the
            distro package (named `asciinema` on RPM, DEB, and PAC alike). It
            provides the /usr/bin/asciinema CLI whose `rec` subcommand captures a
            terminal session -- timing, input, and output -- into a replayable
            asciicast v2 .cast file; this is the engine behind the `record:` check
            verb's terminal mode (`record: start` with `record_mode: terminal`,
            served out-of-process by candy/plugin-record). It also installs the
            agg gif generator (asciinema/agg, pinned v1.9.0) as a prebuilt binary
            at /usr/local/bin/agg plus a monospace font (DejaVu Sans Mono) so a
            stopped .cast recording can be rendered to an animated GIF -- the
            engine behind the `record: gif` method. agg is NOT a distro package
            (absent from RPM/DEB/PAC repos), so the candy downloads the pinned
            prebuilt release binary; the font IS a distro package, resolved
            per-distro via package_map (dejavu-sans-mono-fonts / ttf-dejavu /
            fonts-dejavu-core).
        require:
            # plugin-record is the OUT-OF-TREE plugin that serves the `record:` check verb
            # (the asciinema/wf-recorder/pixelflux recording driver lives there, not in
            # charly's core check surface). asciinema PROVIDES the terminal recorder this verb
            # drives, so requiring plugin-record puts it in every composing image's scanned
            # candy set; the host's collectReferencedPluginWords then build-connects it before
            # a baked `record:` check dispatches — otherwise it SKIPs with `unknown verb
            # "record"`. (box/<distro> adopts the matching @github pin via reconcile later — the
            # deliberate cross-repo lag; the R10 bed loads the LOCAL candy via the superproject
            # override.)
            - '@github.com/opencharly/plugin-record/candy/plugin-record:v2026.237.1427'
        package:
            - asciinema
        distro:
            arch:
                package:
                    - ttf-dejavu
            debian,ubuntu:
                package:
                    - fonts-dejavu-core
            fedora:
                package:
                    - dejavu-sans-mono-fonts
        var:
            AGG_VERSION: v1.9.0
        plan:
            - run: download the agg gif generator binary and install it to /usr/local/bin
              download: "https://github.com/asciinema/agg/releases/download/${AGG_VERSION}/agg-${BUILD_ARCH}-unknown-linux-gnu"
              to: /usr/local/bin/agg
              mode: "0755"
              run_as: root
              # Explicit uninstall list so `charly fleet del` removes just the
              # binary this download drops in /usr/local/bin, not the whole shared
              # dir (which would wipe every system-local binary).
              uninstall:
                - /usr/local/bin/agg
            - check: the asciinema recorder binary is installed at /usr/bin/asciinema
              file:
                file: /usr/bin/asciinema
                exists: true
            - check: the asciinema package is registered in the distro package database
              package:
                package: asciinema
                installed: true
            - check: running asciinema --version prints the asciinema version banner and exits cleanly
              exit_status: 0
              stdout:
                - contains: asciinema
              command: /usr/bin/asciinema --version
            - check: asciinema --help advertises the rec subcommand that captures terminal sessions
              exit_status: 0
              stdout:
                - contains: rec
              command: /usr/bin/asciinema --help
            - check: the agg gif generator binary is installed at /usr/local/bin/agg
              file:
                file: /usr/local/bin/agg
                exists: true
            - check: agg reports its version and exits cleanly
              exit_status: 0
              stdout:
                - contains: agg
              command: /usr/local/bin/agg --version
            - check: agg converts a minimal asciicast to a gif (proving the binary and the monospace font work)
              exit_status: 0
              command: |
                printf '{"version": 2, "width": 20, "height": 5, "timestamp": 0}\n[0.0, "o", "hello\\n"]\n' > /tmp/charly-agg-check.cast && /usr/local/bin/agg /tmp/charly-agg-check.cast /tmp/charly-agg-check.gif && test -s /tmp/charly-agg-check.gif

asciinema-skill:
    skill:
        name: asciinema
        family: coder
        owner: asciinema
        description: |-
            Terminal session recorder (asciinema).
            Use when working with the asciinema candy.
        content: |-
            # asciinema -- Terminal session recorder
            
            ## Candy Properties
            
            | Property | Value |
            |----------|-------|
            | Install files | `charly.yml` (packages + one binary download) |
            | Depends | none |
            
            ## Packages
            
            RPM: `asciinema` + `dejavu-sans-mono-fonts` · PAC: `asciinema` + `ttf-dejavu` · DEB: `asciinema` + `fonts-dejavu-core`
            
            ## What It Does
            
            Records terminal sessions as `.cast` files (asciicast v2 format). Recordings capture timing, input, and output — can be replayed with `asciinema play`, uploaded to asciinema.org, or converted to GIF/video. Also installs the `agg` gif generator (asciinema/agg, pinned v1.9.0, prebuilt binary at /usr/local/bin/agg) plus a DejaVu Sans Mono font so a stopped recording can be rendered to an animated GIF — the engine behind the `record: gif` method.
            
            ## Cross-distro coverage
            
            RPM: `asciinema` · PAC: `asciinema` · DEB: `asciinema` — full parity.
            
            ## Usage
            
            ```yaml
            # box charly.yml — compose the candy as an inline list in the box body
            my-box:
                candy:
                    base: fedora
                    candy: [asciinema]
            ```
            
            Used by the `record:` check verb (a `record: start` step with `record_mode: terminal`) for terminal recording sessions. Also available standalone via `asciinema rec`.
            
            ## Integration with the `record:` check verb
            
            Author ordered `record:` plan steps (the declarative verb served out-of-process by
            `candy/plugin-record` — there is no host `charly check` subcommand for it) and run them
            with `charly check live <image> --filter record`:
            
            ```yaml
            plan:
                - check: a terminal recording starts
                  record:
                      method: start
                      record_name: demo
                      record_mode: terminal
                  context: [deploy]
                - check: a command is sent into the recording
                  record:
                      method: cmd
                      record_name: demo
                      text: echo hello
                  context: [deploy]
                - check: the recording stops and is copied out
                  record:
                      method: stop
                      record_name: demo
                      artifact: demo.cast
                  context: [deploy]
            ```
            
            ```bash
            charly check live <image> --filter record   # runs the steps above
            asciinema play demo.cast                     # play back the copied-out artifact
            ```
            
            ## Converting a recording to an animated GIF
            
            The `record: gif` method (served by the same plugin) renders a stopped
            terminal recording to an animated GIF with agg — the binary this candy
            installs. It runs agg in the container and copies the .gif to the host
            artifact path:
            
            ```yaml
            plan:
                - check: the recording is rendered to an animated gif
                  record:
                      method: gif
                      record_name: demo
                      artifact: demo.gif
                      theme: monokai
                      speed: 2
                      idle_time_limit: 1
                  context: [deploy]
            ```
            
            agg options (theme, font_size, speed, idle_time_limit, fps_cap, select,
            cols, rows, no_loop, last_frame_duration, renderer) map 1:1 to agg's CLI
            flags; the recording's embedded theme is used when `theme:` is unset.
            
            ## Note
            
            Also available via the `dev-tools` candy (which includes asciinema among many other tools). This standalone candy is for boxes that need terminal recording without the full dev-tools bundle.
            
            ## Used In Boxes
            
            - `/charly-selkies:sway-browser-vnc` (via `sway-desktop` metalayer)
            - `/charly-selkies:selkies-labwc` (via `selkies-desktop` metalayer)
            - `/charly-selkies:selkies-labwc-nvidia` (via `selkies-desktop` metalayer)
            
            ## Related Commands
            
            - `/charly-check:record` — Terminal recording via asciinema (start, stop, cmd, gif)
            
            ## Cross-References
            
            - `/charly-check:record` — the `record:` check verb (`record_mode: terminal` records, `record: gif` renders) uses asciinema + agg
            - `/charly-coder:dev-tools` — Also includes asciinema (larger candy)
            
            ## When to Use This Skill
            
            Use when the user asks about:
            - Terminal session recording
            - Converting a terminal recording to an animated GIF
            - asciinema in containers
            - The `asciinema` candy
            
            ## Related
            
            - `/charly-image:layer` — candy authoring reference (`charly.yml` schema, plan-step verbs, service declarations)
            - `/charly-check:check` — declarative testing (`check:` block, `charly check box`, `charly check live`)
